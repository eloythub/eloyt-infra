apiVersion: v1
kind: Service
metadata:
  namespace: api
  name: el-mongo-lb
  labels:
    app: el-mongo-lb
spec:
  type: LoadBalancer
  loadBalancerIP: 104.199.218.233
  ports:
    - port: 27017
      name: http
  selector:
    app: el-mongo